from tkinter import*

root = Tk()
root.title("tkinter")
root.geometry('640x480+1000+400') # width * height + x axis + y axis
root.resizable(False, False) # can't resize frame (width, height)
root.mainloop()
